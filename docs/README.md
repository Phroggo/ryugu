# Documentation

- `word/mantis_draft_2.docx` (and `.pdf`) — the submitted paper.
- `*.png`, `reaction_wheel_photo.jpg` — figures used in the paper, as
  standalone files.
- `paper_assets/` — supporting evidence: derivations and calculations
  (`calculations/`), raw simulation captures and telemetry logs
  (`sim_captures/`), and reference sketches (`sketches/`).
